%h1 Show All Bus Stops
%h4= link_to 'Add new bus stop.', controller: 'busstop', action: 'new'
- if @busstops.blank?
	%p No bus stop currently exist.
-else
	%p 
		These are the bus stops
	%table.table.table-hover.table-condensed.text-center
		%tr
			%th.text-center
				Bus Stop Name
			%th.text-center
				Bus Stop LatLong
			%th.text-center
				Bus Stop Secondary Name
			%th.text-center
				Options
		- @busstops.each do |c|
			%tr
				%td
					= link_to c.busStopName, {:action => 'view', :id => c.id}
				%td
					= c.busStopLatLong
				%td
					= c.busStopSecName
				%td
					= link_to busstop_view_path(c.id) do
						= content_tag :span, class: "glyphicon glyphicon-list" do
							=""
					|
					= link_to do
							= content_tag :sapn, class: "glyphicon glyphicon-ok" do
								=""
