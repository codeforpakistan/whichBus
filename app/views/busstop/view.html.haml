%h3 Viewing Busstops
=link_to 'Show All Bus Stop', {:action => 'showAll'}
- if @busstop.blank?
	%p There is no bus stop currently exist
-else
	%p These are the bus stops
	%table.table.table-hover.table-condensed.text-center
		%tr
			%th.text-center
				Bus Stop Name
			%th.text-center
				Bus Stop LatLong
			%th.text-center
				Bus Stop Secondary Name
			%th.text-center
				Options
		%tr
			%td
				=@busstop.busStopName
			%td
				=@busstop.busStopLatLong
			%td
				=@busstop.busStopSecName
			%td
				= link_to busstop_delete_path, method: :delete, data: { :confirm => 'Are you sure?' } do
					= content_tag :span, class: "glyphicon glyphicon-remove" do
						=""
%br



