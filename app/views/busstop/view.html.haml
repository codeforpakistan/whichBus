%h3 Bus-Stop Detail
=link_to 'Show All Bus Stop', busstop_showAll_path, :class => "btn btn-primary" 
- if @busstop.blank?
	%p There is no bus stop currently exist
-else
	%p.text-center
		These are the list of bus stops
	%br
	%table.table.table-hover.table-condensed.text-center
		%tr
			%th.text-center
				Bus Stop Name
			%th.text-center
				Options
		%tr
			%td
				=@busstop.busStopName
			%td
				= link_to busstop_delete_path, method: :delete, data: { :confirm => 'Are you sure?' } do
					= content_tag :span, class: "glyphicon glyphicon-remove" do
						=""
%br



